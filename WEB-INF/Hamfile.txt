set names utf8;

drop database if exists Hamfile;
create database Hamfile;

grant all privileges on Hamfile.* to kunugi@localhost identified by 'hamuro';
use Hamfile;

create table person(id char(32) primary key, password char(60), name char(32),imagepath char(255));

insert into person(id, password, name)
		values("HAMURO","GOD","REI HAMURO");